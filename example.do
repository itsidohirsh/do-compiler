prog my_prog:
	int num;
	set num = 0;

	if (num == 0):
		int num;
		int y;
		if (num == 2):
		 	int y;
			if (y):
				int y;
				int x;
				set y = x + 1;
			done
			int x;
			set y = x - num;
		done
		char cha;
		if (num == 3):
			set num = cha * 3;
		done
	done
	else:
		char ch;
		if (ch):
			char ch1;
			int num;
		done
	done
	int ido;
done :)
