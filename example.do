prog my_prog:
	int num;
	char hi;
	set hi = 7;
	set num = (-(num)*!hi*(3-2)/4&&7);
	
	while (num < 10):
		set num = num + 1;
	done

	int result;
	char i;
	set result = 0;
	set i = '0';

	int ido;
	if (result == 0 && i == 'A'):
		char wow;
		if (wow * 2 == 30 && 14):
			set ido = 2;
		done
		else:
			set ido = 1;
		done

		set result = 1 - result;
		set i = '1';
	done
	else:
		set result = 1 - result;
		set i = '0';
	done
done :)
