prog simple:
	num xaa;
	set xaa = 3;

	if (x == 5):
		num y;
		set y = x * 3;
	done
	
	else:
		num w;
		num z;
		set w = 5;
		set z = 28;

		char ch;
		set ch = '3';
	done

	# // error check

	num do;
	set do = 0;
	num count;
	set count = 0;

	while (do <= 10):
		count = count + 1;
		do = do + 1;
	done

done :)
